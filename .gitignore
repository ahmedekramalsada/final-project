# --- OS & IDE ---
.DS_Store
.vscode/
.idea/
*.swp

# --- Node.js ---
node_modules/
npm-debug.log
yarn-error.log
dist/
build/
coverage/

# --- Terraform ---
**/.terraform/
*.tfstate
*.tfstate.backup
# .terraform.lock.hcl should be committed for team consistency, but can be ignored if preferred.
# Keeping it allows locking provider versions.

# --- Secrets & Environment Variables (CRITICAL for Security) ---
*.tfvars
*.tfvars.json
.env
.env.local
.env.*
*.pem
*.key
creds/
secrets/
*.log

# --- Crash Logs ---
crash.log
